<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nablanet.aula31">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".MainApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppBaseTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".export.ExportActivity"
            android:label="@string/title_activity_export"
            android:theme="@style/AppBaseTheme.NoActionBar">
        </activity>
        <activity android:name=".schedules.ScheduleActivity">
        </activity>
        <activity
            android:name=".schedules.CourseScheduleActivity"
            android:label="@string/title_activity_course_schedule"
            android:theme="@style/AppBaseTheme.NoActionBar" />
        <activity
            android:name=".courses.CourseActivity"
            android:label="@string/title_activity_courses"
            android:theme="@style/AppBaseTheme.NoActionBar" />
        <activity
            android:name=".tracking.MemberTrackActivity"
            android:label="@string/title_activity_person"
            android:theme="@style/AppBaseTheme" />
        <activity
            android:name=".schedules.ScheduleCalendarActivity"
            android:label="@string/title_activity_schedule"
            android:theme="@style/AppBaseTheme" />
        <activity
            android:name=".classes.ClassActivity"
            android:label="@string/title_activity_schedule"
            android:theme="@style/AppBaseTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PhoneAuthActivity" />
        <activity android:name=".UserActivity" />
    </application>

</manifest>